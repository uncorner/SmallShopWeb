version: '3'
services:
  catalog-api-app:
    container_name: catalog-api
    build:
      context: .
      dockerfile: Catalog\Dockerfile
    ports:
      - "5000:80"
    networks:
      - smallshop-network
networks:
  smallshop-network:
    driver: bridge
